{
  "name": "ayushpatraphotography",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "next lint",
    "test": "vitest run",
    "bundle-worker": "node scripts/bundle-worker.mjs",
    "preview": "npx @cloudflare/next-on-pages && node scripts/bundle-worker.mjs && wrangler dev",
    "deploy": "npx @cloudflare/next-on-pages && node scripts/bundle-worker.mjs && wrangler deploy"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "next": "^15",
    "react": "^18",
    "react-dom": "^18"
  },
  "devDependencies": {
    "@cloudflare/next-on-pages": "^1",
    "@cloudflare/workers-types": "^4",
    "@types/node": "^20",
    "@types/react": "^18",
    "@types/react-dom": "^18",
    "eslint": "^8",
    "eslint-config-next": "^14",
    "typescript": "^5",
    "vitest": "^4.1.6",
    "wrangler": "^3"
  }
}
