# Copy to .env.local for local development
# These must ALSO be set in Cloudflare Pages dashboard (Settings > Environment variables)

ADMIN_PASSWORD=your_secret_password_here
AUTH_SECRET=replace_with_random_32_char_string
